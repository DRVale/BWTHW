specVersion: "v1"
serviceId: "projectapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "projectapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
